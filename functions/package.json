{
  "name": "cfsb-dashboard-coach-functions",
  "description": "Private backend for CFSB Dashboard Coach",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js && node --check client-contract.js && node --check questionnaire-link.js && node --check access-control.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@google/genai": "^2.11.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
