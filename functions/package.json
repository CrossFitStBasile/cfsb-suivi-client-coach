{
  "name": "cfsb-dashboard-coach-functions",
  "description": "Private backend for CFSB Dashboard Coach",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "lint": "node --check index.js && node --check sync-status.js && node --check task-import.js",
    "test": "node --test"
  },
  "dependencies": {
    "@google/genai": "^2.11.0",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5"
  }
}
